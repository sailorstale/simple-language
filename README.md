# Simple Language

**Скил для русского** | [English skill](README.en.md)

Правила ясного русского письма для Claude Code.

## Боль

Claude по умолчанию пишет сложно. Предложения выходят длинными, специальные слова идут без пояснений, а мысли сжимаются в плотные тезисы. Понять такой текст можно, но приходится вчитываться и перечитывать.

## Решение

Этот набор правил учит Claude писать понятно. Он объясняет сложные вещи обычными словами и щедро форматирует текст, поэтому читатель схватывает смысл с первого раза.

Правила лежат в папке `~/.claude` и подключаются к Claude Code один раз. Дальше Claude пишет по ним сам, и просить его об этом в каждом разговоре не нужно.

## Три примера: было и стало

Правил в наборе двадцать про язык и ещё одиннадцать про то, как оформлять текст. Проще один раз увидеть, что они меняют.

**Веди с главного.** Первое предложение отвечает на вопрос, а подробности идут после него.

> 🚫 «Идемпотентный импорт с детерминированными id и upsert родительских записей позволяет…»
>
> ✅ «Данные загрузились. Можно перезапускать сколько угодно, повторов не появится.»

**Верни глаголу работу.** Из существительных вроде «осуществление» и «проведение» вынули действие, и фраза стала казённой.

> 🚫 «Является организацией, осуществляющей помощь людям.»
>
> ✅ «Помогает людям.»

**Дай числу смысл.** Голая цифра ничего не говорит, а доля читается сразу.

> 🚫 «Собрано 39% плана.»
>
> ✅ «Собрано примерно 4 части из 10, то есть меньше половины того, что планировали за год.»

## Несколько правил форматирования

Половина работы приходится на то, как текст разложен на странице. Вот пять правил из этой части набора.

- **Абзац держится в двух-четырёх предложениях**, а новый топик или шаг начинается с нового абзаца.
- **Пункт списка помещается в одно предложение**, и если он длиннее, значит это был абзац, а подробности идут вложенными подпунктами.
- **Списка из одного пункта не бывает**, потому что список показывает, что пунктов несколько и они равноправны.
- **Структуру держат заголовки**, выделенная жирным строка их не заменяет, а уровни идут по порядку: за `##` следует `###`.
- **Текст ссылки называет то, куда она ведёт**, поэтому слова «здесь», «тут» и «подробнее» в ссылку не ставят.

Остальные правила разбирают картинки и подписи к ним, устройство таблицы, работу каждого приёма оформления и доступность текста для тех, кто читает его не глазами.

## Источники

Правила не придуманы с нуля. Они собраны из руководств, которыми пользуются государства и крупные компании.

- **Правила языка** сделаны по мотивам инфостиля Максима Ильяхова: сервис [Главред](https://glvrd.ru) и книга «Пиши, сокращай».
- **Правила форматирования** опираются на руководства [Microsoft](https://learn.microsoft.com/en-us/style-guide/welcome/) и [Google](https://developers.google.com/style), а также на исследования [Nielsen Norman Group](https://www.nngroup.com/articles/how-users-read-on-the-web/) о том, как люди читают с экрана.

## Как это работает

Набор состоит из трёх частей, и каждая делает свою работу.

- **Хук** из папки `hooks/` подкладывает Claude короткую выжимку правил при каждом твоём сообщении, чтобы они не забывались в обычном разговоре.
- **Скил** из папки `skills/pishi-prosto/` хранит все правила целиком и открывается, когда Claude берётся за большой текст или когда ты зовёшь его командой `/pishi-prosto`.
- **Проверщик** из папки `skills/pishi-prosto/scripts/` читает уже написанный файл и показывает места, где правила нарушены.

Три части закрывают разные дыры. Хук приходит всегда, но несёт только девять главных пунктов. Скил объясняет подробно, зато Claude открывает его редко. А проверщик ловит то, что всё равно проскочило, потому что автор своих ошибок в тексте не видит.

Проверку запускают руками, когда документ готов:

```bash
node ~/.claude/skills/pishi-prosto/scripts/prose-check.mjs "путь/к/файлу.md"
```

Она ничего не чинит и не всегда права, поэтому решает человек.

> Скилы подхватит любой агент на Claude Code, а хук работает только в самом Claude Code.

## Установка

Проще всего попросить Claude. Склонируй репозиторий, открой папку в Claude Code и скажи: «установи эти правила письма себе». Claude прочитает файлы, скопирует скил и хук в `~/.claude` и пропишет `settings.json`.

```bash
git clone https://github.com/sailorstale/simple-language.git
```

Дальше идут подробности на случай, если ставишь без Claude или хочешь понять, что именно происходит. Обычному пользователю эти настройки трогать не нужно, Claude делает их за него.

### Установщик

Запусти скрипт. Он спросит язык и сам всё скопирует и подключит.

```bash
cd simple-language && ./install.sh
```

### Вручную по шагам

1. **Скопируй скил** в папку скилов Claude:

```bash
cp -R skills/pishi-prosto ~/.claude/skills/
```

2. **Скопируй хук** и сделай его исполняемым:

```bash
cp hooks/write-simply-reminder.sh ~/.claude/hooks/ && chmod +x ~/.claude/hooks/write-simply-reminder.sh
```

3. **Подключи хук** в `~/.claude/settings.json`: добавь блок из [settings-snippet-ru.json](settings-snippet-ru.json).

> Если раздел `hooks` в файле уже есть, впиши запись `UserPromptSubmit` внутрь него. Второй раздел `hooks` заводить не нужно. Хук нужен один, два одновременно подключать не надо.

После этого перезапусти сессию Claude, чтобы он подхватил новый хук и увидел скил.

## Лицензия

MIT. Пользуйся свободно, меняй под себя, делись дальше. Полный текст лежит в файле [LICENSE](LICENSE).
